# v0.6.7 릴리즈 노트

## iOS · Android

### 버그 수정

#### 충전 중 주행 세션으로 오기록되는 문제 수정
- 완속·급속충전기에 꽂혀 있어도 BYD API가 `powerGear=3`을 유지해 `isDriving=true`로 잘못 판정되던 문제 수정
- **원인**: `isDriving = powerGear == 3 || speed > 0` 로직에서 충전기 연결 상태를 구분하지 못함
- **영향**: 충전 중 `isDriving=true` → `isCharging=false` → 충전 세션 생성 불가, 충전 기록 누락
- **수정**: `speed > 0` → 주행, `instantPowerW > 0` → 충전(isDriving=false), 나머지 → powerGear로 판단

#### 회생제동 중 충전 세션으로 오기록되는 문제 수정
- 주행 중 감속하면서 speed=0이 되는 순간 `gl`이 양수(회생제동)이면 충전기 연결로 오판 → 충전 세션이 잠깐 기록되던 버그 수정
- **원인**: `instantPowerW > 0 → isDriving=false` 로직이 충전기 연결과 회생제동을 구분하지 못함
- **수정**: 이전 폴링에서 주행 중이었고 `speed=0` + `gl>0`이면 회생제동으로 판단 → `isDriving=true` 유지

---

# v0.6.6 릴리즈 노트 (Android)

## 버그 수정

### 완속 충전 세션 미기록
- 완속 충전 중 BYD API가 `isCharging=false`를 반환하면서 SOC 변화도 없을 때 충전 세션이 기록되지 않던 버그 수정
- **원인**: `apiCharging=false`를 "충전 아님 확정"으로 취급 → 폴링 간격(5분) 내 SOC 미변화 → 세션 시작 불가
- **수정**: `apiCharging` 값에 무관하게 SOC 상승이면 충전 시작, 이전 충전 중이고 SOC 비하락이면 세션 유지

### 설정 화면 버전 정보 미갱신
- 설정 화면 하단 버전 표시가 하드코딩된 값으로 고정되어 업데이트 후에도 이전 버전이 표시되던 문제 수정
- 설치된 APK의 실제 버전(`BuildConfig.VERSION_NAME`)으로 동적 표시

---

# v0.6.5 릴리즈 노트 (Android)

## 새 기능

### 인앱 APK 업데이트
- 설정 → 앱 업데이트 → **업데이트 확인** 버튼으로 새 버전 수동 확인
- 앱 실행 시 새 버전이 있으면 자동으로 업데이트 다이얼로그 표시 (6시간 쿨다운)
- 다이얼로그에서 릴리즈 노트 확인 후 **다운로드 · 설치** 버튼 한 번으로 업데이트
- 다운로드 진행률 실시간 표시
- "나중에" 선택 시 해당 버전 스킵, 다음 버전부터 다시 알림

---

# v0.6.4 릴리즈 노트 (Android)

## 버그 수정

### 야간 완속 충전 세션 미기록
- 완속 충전기에서 순시 전력(`gl`) 값이 0으로 수신되는 경우 충전 세션이 생성되지 않던 버그 수정
- 주차 중 폴링마다 `chargingState` API를 추가 조회하여 `gl=0` 상황에서도 충전 여부를 정확히 판단

## 개선

### APK 업데이트 시 서명 불일치 해소
- CI 빌드를 debug → release로 전환, 고정 keystore 서명 적용
- 동일 패키지명(`com.ggpark.bydstats`)으로 재설치 없이 업데이트 가능
- ⚠️ 기존 debug 빌드(`com.ggpark.bydstats.debug`) 설치 사용자는 **1회 재설치** 필요

### 세션 데이터 자동 백업
- Google 클라우드 백업으로 충전·주행·배터리 이력 유지
- 동일 Google 계정으로 재설치 시 세션 데이터 자동 복원
- 로그인 정보(아이디·비밀번호·토큰)는 보안상 백업 제외 → 재설치 후 재로그인 필요

---

# v0.6.2 릴리즈 노트 (Android)

## 버그 수정

### 주행/충전 세션 목록 미표시 (Android)
- 세션 탭을 데이터가 없는 상태에서 한 번이라도 열면 이후 세션이 정상 저장되어도 화면에 나타나지 않던 버그 수정
- 주행 세션, 충전 세션 모두 해당

### 배터리 이력 필터 기준 시간 고정 (Android)
- 24시간/7일/30일 필터의 기준 시점이 화면을 처음 연 시각으로 고정되어 장시간 사용 시 최신 데이터가 누락되던 문제 수정

---

# v0.6.1 릴리즈 노트

## iOS

### 버그 수정
- **충전 세션 분리 현상** — 야간 완속 충전 중 세션이 여러 개로 쪼개지던 버그 수정
  - 원인: 순시 전력(`gl`) 값이 일시적으로 0을 반환할 때 충전 종료로 오판
  - 수정: `chargingState` API 결과와 SOC 상승 패턴을 추가 보조 신호로 활용
  - 포그라운드 폴링(앱 실행 중)과 백그라운드 갱신(BGAppRefresh) 모두 적용
- **라이브 액티비티 안정성 개선**
  - 앱 재시작 후 진행 중이던 라이브 액티비티가 복원되지 않던 문제 수정
  - 주행·충전 세션 전환 시 간헐적으로 라이브 액티비티가 시작되지 않던 문제 수정
  - 배터리 0%(API 미준비) 상태에서 라이브 액티비티가 잘못 종료되던 문제 수정
  - 백그라운드 갱신 시 주행·충전 종료 후 라이브 액티비티 자동 종료

### 내부 변경
- MQTT 세션 등록 코드 완전 제거

## Android

### 버그 수정
- **폴링 서비스 백그라운드 지속 실패** — 시스템 재시작 후 폴링이 재개되지 않던 핵심 버그 수정
- **Android 11 이하 앱 즉시 종료** — Dynamic Color 미지원 기기에서 발생하던 크래시 수정
- **로그아웃 버튼 미동작** — 설정 화면에서 로그아웃 버튼 클릭이 동작하지 않던 버그 수정

### 앱 로그 화면 연동
- 설정 → 앱 로그에서 폴링 결과, 세션 시작·종료, 오류를 실시간 확인 가능

### 안정성
- 배터리 최적화 예외 요청 추가 (삼성 One UI 등 공격적 절전 정책 대응)
- 서비스 재시작 시 좀비 코루틴 누적 방지
- GPS 거리 계산 스레드 안전성 개선
- MQTT 관련 코드 완전 제거

---

# v0.4.0 릴리즈 노트

## 새 기능

### 시간대별 전기요금 (iOS · Android)
- 충전기 유형 선택으로 요금제 변경 가능
  - **한전 비공용 저압** (가정용 완속, 기본값): 계절·시간대별 차등 요금
  - **한전 비공용 고압** (상업용 고압): 계절·시간대별 차등 요금
  - **직접 입력**: 비공용 이외 충전기 사용 시 단가 직접 입력
- 설정 화면에서 현재 계절 기준 시간대 요금 테이블 실시간 표시
- 충전 비용 계산이 **충전 시작 시간 기준 시간대 요금**으로 정확해짐
  - 예) 야간(22시~08시) 충전 시 경부하 요금 자동 적용
- 요금 데이터: 한국전력 전기자동차 충전전력요금(자가소비용) 2026.4.16 시행 기준

### 앱 로그 화면 (Android)
- 설정 → 진단 → 앱 로그에서 폴링·세션·GPS 이벤트 실시간 확인
- 공유 버튼으로 로그 파일 외부 전송 가능

### GPS 주행거리 트래킹 (Android)
- 주행 세션 시작 시 자동 GPS 트래킹 시작
- ODO 데이터 없을 때 GPS 거리를 주행 세션에 기록

### 홈 위젯 (Android)
- 앱 없이 배터리·충전 상태·주행 가능 거리 홈 화면 확인

---

## 버그 수정

### 주행 세션 중복 생성 버그 (iOS · Android)
- **원인**: 네트워크 오류(cancelled / timeout) 발생 시 즉시 throw → isDriving=false 오판 → 세션 종료 후 재시작
- **수정**: 마지막 5번째 시도까지 재시도 후 throw, 오류 세션은 노이즈 필터(2분 미만) 로 자동 삭제

### 데이터 기록 불량 버그 (iOS · Android)
- **원인**: `vehicleRealTimeResult`에서 `soc=nil` 응답을 배터리 0%로 기록
- **수정**: soc 필드 없으면 재시도, 0% 데이터 포인트 필터링

### JSON String 타입 숫자 파싱 실패 (Android)
- BYD API가 숫자를 `"75"` 형태의 String으로 반환하는 경우 파싱 실패 → 0 처리되던 문제 수정

### 충전·주행 세션 에너지/비용 계산 오류 (Android)
- 세션 종료 시 `endSoc` 미업데이트 → `energyKwh=0`, `estimatedCostKrw=0` 버그 수정

---

## 기술 변경

- 화이트박스 테스트 추가 (Android): VehicleStatusTest, VehicleStatusParserTest, SessionDetectorTest, WidgetDataStoreTest
- 파싱 로직(`parseVehicleStatus`, `jsonInt`, `jsonDouble`) 파일 레벨 분리로 테스트 가능성 개선

---

## 플랫폼별 지원 현황

| 기능 | iOS | Android |
|---|:---:|:---:|
| 실시간 차량 상태 | ✓ | ✓ |
| 충전·주행 세션 기록 | ✓ | ✓ |
| 시간대별 전기요금 | ✓ | ✓ |
| GPS 주행거리 | ✓ | ✓ |
| 홈 위젯 | ✓ | ✓ |
| 라이브 액티비티 | ✓ | — |
| 앱 로그 화면 | — | ✓ |
