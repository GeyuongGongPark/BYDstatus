# v0.4.0 릴리즈 노트

## 새 기능

### 시간대별 전기요금 (iOS · Android)
- 충전기 유형 선택으로 요금제 변경 가능
  - **한전 비공용 저압** (가정용 완속, 기본값): 계절·시간대별 차등 요금
  - **한전 비공용 고압** (상업용 고압): 계절·시간대별 차등 요금
  - **직접 입력**: 비공용 이외 충전기 사용 시 단가 직접 입력
- 설정 화면에서 현재 계절 기준 시간대 요금 테이블 실시간 표시
- 충전 비용 계산이 **충전 시작 시간 기준 시간대 요금**으로 정확해짐
  - 예) 야간(22시~08시) 충전 시 경부하 요금 자동 적용
- 요금 데이터: 한국전력 전기자동차 충전전력요금(자가소비용) 2026.4.16 시행 기준

### 앱 로그 화면 (Android)
- 설정 → 진단 → 앱 로그에서 폴링·세션·GPS 이벤트 실시간 확인
- 공유 버튼으로 로그 파일 외부 전송 가능

### GPS 주행거리 트래킹 (Android)
- 주행 세션 시작 시 자동 GPS 트래킹 시작
- ODO 데이터 없을 때 GPS 거리를 주행 세션에 기록

### 홈 위젯 (Android)
- 앱 없이 배터리·충전 상태·주행 가능 거리 홈 화면 확인

---

## 버그 수정

### 주행 세션 중복 생성 버그 (iOS · Android)
- **원인**: 네트워크 오류(cancelled / timeout) 발생 시 즉시 throw → isDriving=false 오판 → 세션 종료 후 재시작
- **수정**: 마지막 5번째 시도까지 재시도 후 throw, 오류 세션은 노이즈 필터(2분 미만) 로 자동 삭제

### 데이터 기록 불량 버그 (iOS · Android)
- **원인**: `vehicleRealTimeResult`에서 `soc=nil` 응답을 배터리 0%로 기록
- **수정**: soc 필드 없으면 재시도, 0% 데이터 포인트 필터링

### JSON String 타입 숫자 파싱 실패 (Android)
- BYD API가 숫자를 `"75"` 형태의 String으로 반환하는 경우 파싱 실패 → 0 처리되던 문제 수정

### 충전·주행 세션 에너지/비용 계산 오류 (Android)
- 세션 종료 시 `endSoc` 미업데이트 → `energyKwh=0`, `estimatedCostKrw=0` 버그 수정

---

## 기술 변경

- 화이트박스 테스트 추가 (Android): VehicleStatusTest, VehicleStatusParserTest, SessionDetectorTest, WidgetDataStoreTest
- 파싱 로직(`parseVehicleStatus`, `jsonInt`, `jsonDouble`) 파일 레벨 분리로 테스트 가능성 개선

---

## 플랫폼별 지원 현황

| 기능 | iOS | Android |
|---|:---:|:---:|
| 실시간 차량 상태 | ✓ | ✓ |
| 충전·주행 세션 기록 | ✓ | ✓ |
| 시간대별 전기요금 | ✓ | ✓ |
| GPS 주행거리 | ✓ | ✓ |
| 홈 위젯 | ✓ | ✓ |
| 라이브 액티비티 | ✓ | — |
| 앱 로그 화면 | — | ✓ |
